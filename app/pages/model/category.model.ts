export class Category {
    constructor(categoryCode:number,categoryName:string){
        this.categoryCode = categoryCode;
        this.categoryName = categoryName
    }

    categoryCode: number;
    categoryName: string;
}